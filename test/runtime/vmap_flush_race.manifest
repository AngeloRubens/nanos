(
    boot:(
        children:(
            klib:(children:(
                tmpfs:(contents:(host:ARCH_DIR/bin/tmpfs))
                shmem:(contents:(host:ARCH_DIR/bin/shmem))
            ))
        )
    )
    children:(
        vmap_flush_race:(contents:(host:output/test/runtime/bin/vmap_flush_race))
    )
    klibs:bootfs
    program:/vmap_flush_race
    environment:()
)
